#import pygame
#from .utils import *
#from random import randint
#from main import player, planet 

#pizza_enemy = pygame.image.load('assets/textures/pizza_enemy.png')
#apple_enemy = pygame.image.load('assets/textures/apple.png')
#macaroni_enemy = pygame.image.load('assets/textures/macaroni.png')
#enemy_list = [pizza_enemy, apple_enemy, macaroni_enemy]
#random_enemy = enemy_list[randint(0,3)]
#enemies = []
#def generate_enemy(size=16):
    #enemy = SuperSprite(window, pygame.image.load("assets/textures/projectile.png").convert_alpha())
    #enemies.append({"sprite": enemy, "angle": 0, "start": (planet.size[0]/2 + player.rect.size[0], planet.size[1]/2 + player.rect.size[1] / 2 - player.rect.y + player.ground_y)}) #(planet.size[0]/2 + player.rect.size[0], -planet.size[1]/2 + player.rect.bottom)
    #return enemy


        






